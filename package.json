{
  "name": "@codexteam/icons",
  "version": "0.0.0",
  "main": "dist/icons.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist; yarn generate",
    "generate": "node bin/generate.js"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "svgo": "^2.8.0"
  }
}
